(function(Mootor, window) {

    Mootor.namespace('Mootor.Form');
    Mootor.Form = (function() {        
        return {            
            init: function() {
            }
        };        
    }());   

}(Mootor, window));
