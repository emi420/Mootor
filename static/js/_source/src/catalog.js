(function(Mootor, window) {

    Mootor.namespace('Mootor.Catalog');
    Mootor.Catalog = (function() {
        return {            
            init: function() {
            }
        };        
    }());   

}(Mootor, window));
