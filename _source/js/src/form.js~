/*
 * Mootor Forms (coded by emi420@gmail.com)
 */

(function(Mootor, window, $) {

    /*
     * Private
     */ 
   
    var a = 0,
    b = 0;

    var Form = {
        
        /*
         * Public
         */  
        
        // Show element
        box.test = function(e) {
            console.log("test!");
        };
    };
    
    Mootor.extend(Form);

}(Mootor, window));
